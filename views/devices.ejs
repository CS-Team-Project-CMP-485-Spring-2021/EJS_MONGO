<h1>Devices</h1>

<ul>
    <% devices.forEach(function(device){ %>
      <li><%= device.name %>
        <ul>
            <% Object.keys(devices).forEach(function(device){ %>  /for eavh device its looping through the keys in that device and then display the key
            <li><strong><%= key%> </strong>: <%=device[key] %></li>
            <% }); %>
        </ul>
    </li>
    <% }); %>
  </ul>